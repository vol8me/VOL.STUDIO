node_modules/
dist/
target/
build/
**/pnpm-lock.yaml
**/*.min.js
**/*.min.css
core/public/assets/fonts/

# Tauri CLI/Gradle tarafından üretilir; elle düzenlenen Android kaynakları
# (manifest, Kotlin, tema) bu kapsamın dışında kalır ve normal kapılardan geçer.
**/src-tauri/gen/schemas/
**/src-tauri/gen/android/app/src/main/assets/tauri.conf.json
# Tauri eklenti derlemesi (`tauri-plugin` build betiği) izin başvurusunu ve şemayı üretir.
tauri-v2/plugins/*/permissions/autogenerated/
tauri-v2/plugins/*/permissions/schemas/

# Frozen ürün ağaçları (workspace-lifecycle.json): immutable — rutin format
# kapısı onları taramaz. Yeni frozen paket eklendiğinde buraya da yazılır
# (scripts/quality/tests/frozenToolSelection.test.mjs bunu kilitler).
games/vol-hell/**
games/vol-arachnid/**

# Test coverage raporlari
coverage/

# Araştırma koşularının ÜRETTİĞİ ölçüm çıktıları; elle düzenlenmez ve
# betiğin yazdığı biçim kanıtın kendisidir (F1–F3, F7).

# audio-synth protokolünün YAZDIĞI kanonik belgeler (job kayıtları, manifest).
# Biçimi `prettyCanonicalJson` belirler ve her yazımda aynı baytları verir;
# elle biçimlendirmek özeti değiştirmez ama bir sonraki yazımda boş diff üretir.
devtools/audio-synth/audio-jobs/
devtools/audio-synth/reference/production/manifests/
